# 行尾统一为 LF（DEV-AUDIT-001：修复 CRLF 工作区掩盖 gofmt 检查的问题；
# 仓库索引始终存储 LF，eol=lf 使 checkout 输出 LF，Windows/Linux 行为一致）
*.go text eol=lf

# 文本文件统一 text 属性（autocrlf 归一化基线，防混合行尾）
*.md text
*.json text
*.yml text
*.yaml text
*.py text
*.js text
*.css text
*.html text
*.txt text

# Linux 侧直接执行的部署资产显式 eol=lf（2026-09-03 生产实测：Windows 上 git archive
# 对仅 text 属性的文件按 core.autocrlf 输出 CRLF，VPS bash 执行报 $'\r': command not found；
# eol=lf 使 checkout 与 archive 均输出 LF）
*.sh text eol=lf
Dockerfile text eol=lf
