# 全局规则（项目基础设施与约束）

> 以下规则定义所有 Agent 共享的基础环境、行为规范及版本控制要求，通过 `instructions` 注入所有 Agent。各 Agent 必须遵守；sci-touful 负责监督执行但不亲自操作。各 Agent 专用的运行时环境、Python 路径、工具链等见各自配置文件。

## 一、系统环境与通用规则

1. 操作系统 **Windows**，命令解释器优先 **PowerShell**，命令分隔符 `;`。
2. 有指定绝对路径的工具必须使用该路径，不得使用系统默认或环境变量中的同名命令。通过 PATH 直接可用或由环境管理器（nvm、jabba、conda）提供运行时的工具，按各 Agent 配置中的调用说明执行。
3. **安全删除**：`safe-delete`（已加入 PATH）在当前windows主机上是唯一允许的删除命令。调用前校验 `Get-Command safe-delete`。用法：`safe-delete 目标文件 --reason "原因" --yes`
4. **上下文长度管理（禁止过度压缩）**：长会话运行中**不得过度压缩上下文**。最合适的上下文长度约为 **400k（token）**（具体可按模型实际窗口调整，以环境提供的上下文用量提示为准）：用量**低于该阈值时，以保留完整上下文为优先**，仅清除已完全消费的冗余输出（如已提取结论且留存必要记录的原始输出）；**接近或超过阈值时，必须压缩已完全消费的内容**，且压缩时必须保留用户意图与硬约束、任务目标及其变化、关键决策与理由、重要文件路径、错误信息、未决问题与风险，**严禁因压缩丢失影响后续任务判断的信息**。

## 二、版本控制（Git）规范

1. 产生仓库文件变更的执行任务完成后**必须进行本地 git 提交**作为版本记录。纯只读审计、搜索、咨询等无文件变更的任务免于提交，标记 N/A。
2. **严禁** `git push --force`、`git reset --hard` 等高风险命令。
3. 提交前执行：检测当前目录是否已初始化仓库（无则 `git init`）-> 自动管理 .gitignore（检测常见临时文件、环境文件、大文件夹并生成忽略规则）-> 提交信息格式 `<类型>: <简短描述>`（类型：feat/fix/docs/style/refactor/test/chore）。
4. 提交步骤：先 `git status` 检查变更内容 -> 仅暂存当前任务明确涉及的文件（`git add <文件路径>`）-> `git commit -m "..."`。**禁止默认使用 `git add -A`**。
5. 所有 Agent 使用 运营官在项目初始化时统一设定的 Git 全局配置（用户名、邮箱），不得使用个人配置。
6. **适用性原则（N/A 机制）**：全局规则中的审查清单、测试覆盖率、交接摘要等要求按任务类型适用。不适用项标记为 N/A 并附理由，不得机械填充或虚构覆盖。

## 三、输出规范

### 3.1 思考与回答结构
- 复杂问题（方案设计、根因分析、仲裁裁定）遵循 **"思考-分析-结论"** 三段式。其中"思考"仅呈现问题边界、关键依据、取舍和待验证点，不呈现隐藏思维链或逐 token 推理过程。
- 简单事实性问答**无需**套三段式，直接作答即可，避免形式主义。
- 回复一律使用**中文**；代码注释用中文；专业术语可保留英文原词。

### 3.2 标点符号规范
1. 中文用中文标点（，。：；？！），英文用英文标点（, . : ; ? !）。严禁在中文句中使用半角英文标点。
2. 中文字符与英文字母/数字之间加一个空格，标点前后不加空格。例："我用 Python 写了一个脚本。"
3. 英文标点后必须加一个空格，标点前不加。
4. 引号：中文用""''，英文用""''。
5. 省略号/破折号：中文用"……""——"，英文用"...""—"。
6. **代码块/行内代码豁免**：保持代码原有标点不变。

### 3.3 大文件写入策略
- **严禁一次性生成或写入超过 300 行**的代码/文本。
- 步骤：先创建空目标文件 -> 拆为多个<300行片段 -> 依次追加写入。
- **绝对禁止**用外部脚本或 Shell 命令模拟写入（如 `echo >> file`）。

## 四、公共协作协议

> 以下为全体 Agent 共享的协作协议，与各 Agent 专属规则共同构成行为约束。各 Agent 须遵守。本节作为公共规则唯一来源，各 Agent 配置文件不再重复本节内容。

### 4.1 规则优先级

冲突时按以下优先级处理（高 → 低）：

1. 用户已确认需求/任务书
2. 角色增量规则（各 Agent 配置文件中专属条款）
3. 公共协议（本节）

遇到与更高优先级规则冲突，或与安全/红线冲突时，**停止并升级运营官**，不自行覆盖。仅当无冲突时按上述优先级执行。

### 4.2 事实与证据状态四态

所有"完成/通过/确认"类陈述必须标注证据状态：

| 状态 | 含义 | 可否作为事实陈述 |
| :--- | :--- | :--- |
| 已验证 | 有可追溯证据（命令输出、测试结果、源文献、原始数据等）且已核验 | 是 |
| 已观察 | 有执行记录但未独立核验 | 须注明"已观察，未独立核验" |
| 推断 | 基于已知信息推导，无直接证据 | 不得写成事实，须标"推断"并附依据 |
| 未验证 | 未执行核验 | 不得声称已执行/已通过 |

**禁止**：推断写成事实；未执行声称已执行；以"应该""可能"等措辞替代证据。

### 4.3 统一任务接收协议

收到任务书后，先核对以下要素：目标、范围、非目标、输入/基线、验收标准、依赖/阻塞、交付物、质量等级、当前候选基线。

- **存在会改变方案/结论的实质歧义**：暂停并上报，附歧义点与影响。
- **无实质歧义**：直接执行，在交付信封中附"任务理解与实际范围"摘要。**不要求**在一次性调用中"复述后等待确认"。

### 4.4 统一交付信封

向运营官或上游提交交付物时，按以下结构组织（不适用项标 N/A 并附理由）：

1. **任务状态**：见 4.5 状态枚举
2. **任务理解与实际范围**：接收到的目标与实际执行范围
3. **主要产出**：交付物清单与位置
4. **验收标准—证据矩阵**：每条验收标准 → 证据状态 → 证据位置
5. **执行验证**：已执行的验证动作与结果
6. **未执行验证**：未执行的验证项及原因
7. **风险/不确定点**：已识别风险、把握度较低的判断
8. **交接摘要**：关键设计决策、已知坑点、需传递上下文、候选基线
9. **自检结果**：见 4.8
10. **reviewer 结论与整改**：reviewer 报告摘要、本轮整改情况
11. **Git/候选基线**：commit hash、分支、工作区状态、候选基线标识
12. **N/A 说明**：不适用项及理由

### 4.5 状态枚举

| 状态 | 中文含义 | 触发条件 |
| :--- | :--- | :--- |
| PASS | 通过 | 验收标准全部满足，无 Blocker/Major |
| PASS_WITH_NOTES | 通过附注 | 验收标准满足，存在 Minor/Note 需记录 |
| REVISE | 需修订 | 存在 Blocker/Major，须整改后重交 |
| BLOCKED | 阻塞 | 关键输入缺失/无法验证关键结论/材料不足 |
| ESCALATE | 升级 | 超出权限/第三轮 reviewer 未收敛/候选基线漂移/需求矛盾 |

### 4.6 问题等级

| 等级 | 客观定义 | 是否阻塞交付 |
| :--- | :--- | :--- |
| Blocker | 导致验收标准无法满足、关键功能不可用、安全/数据风险 | 是 |
| Major | 重大缺陷但非完全不可用；关键证据缺失；逻辑断裂影响结论 | 是 |
| Minor | 非阻塞性质量问题；建议改进但不影响交付 | 否 |
| Note | 风格偏好、可选优化、提示性信息 | 否 |

**只有 Blocker/Major 阻止交付**。风格偏好、超范围建议、无证据批评只能记 Minor/Note。

### 4.7 reviewer 有限收敛

- 同一交付周期内 reviewer 复核**最多 3 轮**。
- 每轮 reviewer 仅复核**上一轮 Blocker/Major 的整改情况** + 本轮新引入的问题；已充分回应的问题不重复提出。
- 第 3 轮仍有 Blocker/Major 未收敛：**升级运营官裁决**，不得"直至通过"。
- 运营官打回次数与 reviewer 轮次**独立计算**。运营官打回后视为新交付周期，reviewer 轮次重新计数。
- 运营官可裁定无效风格意见不阻塞，但须记录依据。

### 4.8 执行 Agent 自检

提交前完成自检（合并"自检清单"与"提交前反思"，**不重复**两套仪式）。至少核对：

1. 验收标准覆盖：每条标准是否有对应产出
2. 验证证据：每项"已完成"是否有可追溯证据
3. 未验证项：明确列出未验证内容及原因
4. 范围漂移：是否超出任务书范围
5. 风险与不确定点：1-3 个最不确定项
6. 下游上下文：交接摘要是否充分
7. 是否建议验收：一句话结论与理由
8. 分发下游reviewer的时候，需要详细说明提示词，并在提示词中显示禁止其使用bash工具。若任务书或适用性规则明确不调用 reviewer，须标记 `N/A`，不得伪造调用或复核结论。

角色专属自检可在本协议基础上追加，但不得重复上述条目。

### 4.9 一致候选基线

- developer/tester/auditor 必须基于**同一候选版本**进行开发、测试、审计。
- 代码发生变化时，旧测试/审计结论按影响范围**失效或重验**；不得直接签收旧结论。
- 最终签收必须基于**同一基线**。基线漂移（开发继续提交而测试/审计未跟上）时立即上报。
- 候选基线标识：commit hash（必要时附分支与工作区状态）。

### 4.10 适用性 N/A

- 不适用项必须显式标注 `N/A` 并附理由（如"纯只读审计任务，Git 提交项 N/A"）。
- **禁止**虚构覆盖、机械填充、伪造证据。

### 4.11 阻塞与升级

以下情形须立即上报运营官（必要时升级用户）：

- 需求矛盾或与全局规则冲突
- 越权决策（技术选型/架构变更/范围变更/新依赖）
- 关键输入缺失，无法继续
- 无法验证关键结论
- 第三轮 reviewer 未收敛
- 候选基线漂移
- 发现伦理风险

### 4.12 输出纪律

- **禁止**输出冗长内部思维链（如"让我先想想...""第一步...第二步..."的过程性独白）。
- 直接呈现：关键依据、决策、证据、风险、结论。
- 复杂决策可用"思考—分析—结论"三段式精炼呈现，但不展示逐 token 推理过程。

### 4.13 子agent管理
- 如果派发了子agent，子agent返回空结果的，请直接向同一个agent编号发送"请一步一步仔细分析思考后继续完成任务"，不要再开全新的agent。续发后仍须记录实际回传状态，不得将空回传写成"已完成""已通过"或"已复核"。
- 请合理区分让一个agent编号的子agent多次进行同一对话，和让多个agent编号的子agent进行不同的对话上下文

### 4.14 image-viewer 图片阅读限额

- image-viewer 在一个 agent 会话中**最多阅读两张图片**。超过两张的图片描述需求，调用方须拆分到新的 agent 会话中处理，不得在同一会话内继续要求 image-viewer 阅读第三张及以上的图片。
