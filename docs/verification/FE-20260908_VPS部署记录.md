# FE-20260908 VPS 前端部署记录

## 范围与状态

- 目标站点：`https://safeboard.touful.cn`。
- 上游视觉提交：`91c6c311e9c8caf32a1e20147d95e503216cbdcc`。
- 本次只更新前端及部署验证文档，保留现有 VPS 后端和依赖版本。
- 状态：已于北京时间 2026-09-08 20:53:46 切换到最终前端镜像；前端与发布验证 `PASS_WITH_NOTES`。完整 API 健康不在通过结论内，仍有下述接口问题。

## 分支差异处理

原目录 `/opt/vps-safe-dashboard` 的 HEAD 为 `52df5eb`，与上游分叉，并有大量未提交和未跟踪文件。两个独有历史提交分别是初始发布与 VPS 部署适配；当前工作树还包含后续直接同步的代码。仅依据 Git 的领先、落后数量不能判断实际运行版本。

将上游拉取到独立工作树 `/opt/sentry-releases/20260908-fe91c6c31`，确认新版前端所依赖的部分接口在 VPS 后端不存在。因此未全量切换到上游，也未覆盖原工作树。

另在 `/opt/sentry-releases/20260908-fe-compat` 冻结 VPS 当前运行源码，部署分支为 `codex/deploy-fe-20260908`，基线提交 `7b6b8a1`。视觉更新将在该基线上适配，保留原有页面功能和 API 合约。

## 备份与可恢复性

备份目录：`/opt/sentry-backups/20260908-fe91c6c31`，仅 root 可访问。

| 文件 | 内容与验证 |
| --- | --- |
| `source.tar` | 完整原项目目录，包含 Git、未提交及未跟踪文件；SHA-256 复核通过 |
| `data.tar` | `/var/lib/sentry-agent` 一致性快照；归档可读、SHA-256 复核通过 |
| `image.tar` | 原运行镜像离线备份；SHA-256 复核通过 |
| `config/` | 原 `/etc/sentry-agent` 配置副本，不进入项目仓库 |
| `container-inspect.json`、`image-inspect.json` | 原容器与镜像配置，不进入项目仓库 |
| `worktree.patch`、`git-status.txt` | 原工作树差异和文件状态 |
| `original-binary` | 从原运行容器提取的可执行文件 |
| `data-before-switch.tar` | 20:51:50～20:52:14 补做的最新业务数据快照，SHA-256 与归档可读性已验证 |
| `frontend-release-f886bec.bundle` | 含完整独立部署分支的 Git bundle，可恢复最终源代码历史；`git bundle verify` 通过 |

一致性备份时容器在北京时间 2026-09-08 13:24:16 至 13:24:40 之间暂停约 24 秒，随后恢复。恢复后的健康检查为 `ok=true`，容器未重启。

原镜像 ID：`sha256:46b9425a2baa8741a2a5471c6fd53160ba889f3b5d0ab2b832dc338963bdf1c8`。

回滚保留标签：`sentry-agent:rollback-20260908`。`image.tar` 可在镜像丢失时通过 `docker image load` 恢复。

## 运行基线的动态证据

用冻结的 VPS 源码、原依赖及 Go `1.26.6`，按原构建参数 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64`、`-trimpath -ldflags="-s -w"` 重建。

重建二进制与原运行容器提取二进制 SHA-256 **完全一致**：

`1d552c7725c047b7073c9d4de08a07124c02b116246e1de840a2c39b152765a7`

这项结果将 VPS 工作树源码与实际运行版本联系起来；候选构建仅替换前端静态文件。

测试数据来自上述快照，位于独立目录 `/opt/sentry-staging/20260908-fe91c6c31`。SQLite `PRAGMA quick_check` 返回 `ok`。快照行数如下，数字仅代表快照时刻：

| 表 | 行数 |
| --- | ---: |
| connections | 137,752 |
| ssh_attempts | 36,359 |
| firewall_events | 2,398,623 |
| cred_events | 2,214 |
| resources | 128,763 |

测试配置关闭蜜罐、GeoIP 自动更新、数据保留清理及 SSH 白名单学习；测试使用独立容器网络和可写数据副本，不挂载生产数据或宿主采集源。

## 发布与回滚工具

- `tools/frontend-runtime.Dockerfile`：沿用原生产系统层，仅替换包含新前端的可执行文件，重新赋予原有低端口绑定能力。
- `tools/frontend_vps_prepare.py`：准备本次隔离数据与测试配置，校验快照数据库完整性。
- `tools/frontend_vps_switch.py`：限定本次固定路径、配置哈希与候选镜像 ID，校验生产基线后仅覆盖 Compose 的镜像设置；检查健康、线上资源哈希、挂载和权限；失败自动回滚。

这些工具是本次固定基线的运行记录，不是可直接用于任意服务器的通用部署器。再次部署应重新盘点、备份并更新精确基线。

## 最终验收

首轮候选提交为 `a33192b`。浏览器视觉检查发现后置旧 `.stat { display:flex }` 覆盖新版 grid，修正该规则及卡片最小宽度后，最终候选提交为 `f886bec891ec3754ccaad62fe355a32b8652f49f`。相对于运行基线 `7b6b8a1`，仅修改 `internal/web/static/index.html` 和 `app.js`。

候选镜像：`sha256:5201f02d13bd199692d7cfbbd37df41b32f687e62a84d117e34d6e35dcf5c806`。

候选二进制 SHA-256：`e98d0bb50021ae59984cc4e76b5ef891a20fb98fee9995d31505c22344a6998c`。

| 静态文件 | SHA-256 |
| --- | --- |
| index.html | `9f2b85b73f269ac4f8a766e6a59f3b55b7ae017b80c808bf41c53d5fde00ee18` |
| app.js | `88e12a5f689ff538283d92b91639c89bd07ed8d00c15e49c50e921bc514054b2` |

候选 `go test ./internal/web` 通过。隔离容器 `sentry-fe-stage-20260908` 仅绑定宿主 `127.0.0.1:14002`，健康检查 `ok=true`。CPU 和内存已调整为与生产一致的 1.6 CPU、512 MB；测试仍使用独立数据副本。发布脚本的非切换模式已通过配置哈希、源版本、镜像与资源哈希检查。

### 已有后端限制

上线前在旧生产版本只读复现：`/api/v1/firewall/timeline?range=24h` 返回 HTTP 500，耗时 15.015980 秒；同一时段 `summary?range=1h` 返回 HTTP 200。隔离候选也出现较大时间范围的查询超时及 SQLite 临时空间不足。生产与测试的 `/tmp` 均为 16 MB。

这些结果证明旧生产版本已有 24 小时趋势查询故障，但尚未完成其根因诊断，不能仅凭临时空间报错断定所有超时原因。本次前端发布不修改后端查询、数据库索引、生产资源限制或采集配置。

### 审查与复测边界

独立 reviewer 第一轮确认前端 API 调用集合、查询参数及原有 75 个 DOM ID 均保留；发现发布脚本的版本锁定和异常记录不足。第二轮确认固定基础镜像 digest、完整候选 commit、镜像内二进制 SHA、干净工作树、纯前端范围和主进程监听归属门禁已补齐；指出报告写入失败仍可能阻断回滚，随后已改为 best-effort 记录。

主 Agent 执行 `test_frontend_vps_switch.py` 的 7 项模拟故障测试全部通过，覆盖成功路径、发布失败、健康检查失败、latest 标签失败、回滚失败保留双错误、报告磁盘写失败仍执行回滚、监听端口属于其他进程。该测试为模拟故障注入，不是生产回滚演练。

独立 verifier 对首轮候选实际执行了四页导航、320/390/900/1440 宽度、WS 消息和 CSV 下载验证；各项交互及溢出检查通过，未捕获未处理 JavaScript 异常，但因 HTTP 500/429 判为整体验证失败。之后该 Agent 曾因额度限制中断，主 Agent 接手完整复测；恢复后同一 verifier 又独立验证了最终前端的资源哈希、1440 五列/390 双列 KPI、两档宽度无溢出、`pageerror=0` 和 WS 打开及 170 条消息，结论为视觉子集 `PASS_WITH_NOTES`。

独立原始结果中的 `status=FAIL` 对应持续请求下 `networkidle_after_1h` 超时，不应改写或隐藏；该等待条件不是页面可用性的验收标准。原始结果的 image 字段沿用了首轮镜像 ID，verifier 收尾通过真实 Docker inspect 确认实际测试容器属于最终镜像 `5201f02…`；资源 SHA 始终与最终候选一致。原始数据与解释分别见 `independent-visual-raw.json`、`independent-visual-notes.md`。

第三轮独立 reviewer 返回 `PASS_WITH_NOTES`，最后一项发布 P1 已关闭。附注：报告写入故障时会继续恢复并输出 stderr；本次实际发布报告已正常持久化，不存在该运行故障。

主 Agent 最终隔离浏览器检查：四页 × 四档宽度共 16 组布局、桌面五列 KPI、移动端双列 KPI、真实 CSV 下载、资源哈希、健康与 WS 检查通过，无未处理脚本异常；一次 HTTP 429 单独记录为接口限流附注。证据：`evidence/fe20260908-deploy/candidate.json`。

### 正式切换结果

`frontend_vps_switch.py --apply` 返回 `PASS`，完成容器重建、目标进程与监听确认、资源哈希检查、原挂载/网络/用户/能力/资源限制核对、运行二进制哈希核对，以及 `sentry-agent:latest` 更新。完整结果见 `evidence/fe20260908-deploy/switch.json`。

新容器开始时间：`2026-09-08T12:53:46.500938983Z`（北京时间 20:53:46）。公网健康检查 `ok=true`，schema 仍为 `1`，保留期仍为 7 天，容器重启计数为 0。原 10 个蜜罐端口均由新容器主进程监听。

原项目工作树状态哈希与备份一致，生产配置与 compose 文件哈希保持不变。测试容器已停止，测试副本、旧容器、镜像与备份保留，可用于追溯。

公网 HTTPS 浏览器复验也完成了四页 × 四档宽度、五列/双列 KPI、WS、CSV 下载与资源哈希检查，无未处理脚本异常、无新增 404。报告见 `evidence/fe20260908-deploy/online.json`。连续交互期间观察到 500/429：原始错误清单完整保留，**不能把前端通过解读为所有 API 通过**。

测试结束后的低频只读复核：`summary?range=1h` 为 HTTP 200、0.010685 秒；`resources?range=1h&step=60s` 为 HTTP 200、0.002068 秒。故障日志显示对应查询超时。除了已有生产基线确认的 24h 趋势超时外，其余错误未完成逐一根因诊断；本次没有修改后端 SQL、连接池、限流或生产资源配置。

### 人工回滚与后续发布

若需回滚，在 VPS 执行：

```sh
docker compose -p deploy --project-directory /opt/vps-safe-dashboard/deploy -f /opt/vps-safe-dashboard/deploy/docker-compose.yml -f /opt/sentry-backups/20260908-fe91c6c31/rollback.override.json up -d --no-build --pull never --no-deps sentry-agent
docker image tag sha256:46b9425a2baa8741a2a5471c6fd53160ba889f3b5d0ab2b832dc338963bdf1c8 sentry-agent:latest
curl -fsS http://172.18.0.1:4001/api/v1/health
```

正常回滚仅恢复旧镜像，继续使用当前数据库，不用旧数据快照覆盖新增数据。数据归档仅供独立故障恢复时人工核对后使用。

后续重建本次版本必须使用 `/opt/sentry-releases/20260908-fe-compat` 的 `f886bec` 源码和固定运行镜像，或从上述 bundle 恢复。原 `/opt/vps-safe-dashboard` 是保留的旧工作树，不应在其中直接执行 `--build` 来重建本次新前端。
