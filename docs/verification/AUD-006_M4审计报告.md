# AUD-006 M4 审计报告（DEV-008/DEV-009 放行条件审计）

- 审计对象：M4 本地部分部署资产（deploy/ 套件 + 镜像 + 文档）
- 审计时间：2026-08-14
- 基线：a7b56f0 → 7ae2d99 → DEV-009 修复后新基线
- 关联：AUD-002/AUD-003 交接、reviewer DEV-008 三轮、DEV-009 放行条件

## 1. 审计结论

| 项 | 结论 |
| :--- | :--- |
| M4B-01（Blocker）LOG 前缀格式 | **已修复**（DEV-009）：setup_firewall.sh 前缀改为 `SENTRY_FW:<chain>:<action> `；config.json fw.prefix 与 parse.go 未动；端到端实测（WSL）：插入规则前缀 `SENTRY_FW:input:drop ` → 内核日志同格式 → agent 解析 action=drop，链路通 |
| M4B-02（Major）iptables -P 行偏移 | **已修复**（DEV-009）：`-I INPUT $((ln-1))`（排除 -P 首行）；iptables-legacy 实机验证列入 V-08 复验 |
| F-01（Major）幂等保护 | **已修复**（DEV-009）：nft/iptables 双分支插入前检查 SENTRY_FW 已存在则跳过；WSL 实测重复执行规则数不变（1==1） |
| D-14/F-02 健康检查 | **已修复**（DEV-009）：deploy.sh 改用 `docker exec bash -c 'exec 3<>/dev/tcp/...'`（debian 镜像无 wget/curl，不动镜像） |
| F-03 compose 纵深防御 | **已修复**（DEV-009）：mem_limit 256m + security_opt no-new-privileges；docker compose config 校验通过 |
| F-05 nft 回滚失败告警 | **已修复**（DEV-009）：删除失败输出 [警告] 并列出未删规则与行内容 |
| F-06 fw_log_handles.txt 死代码 | **已修复**（DEV-009）：回滚与插入路径均已移除该文件操作 |
| F-07 journald 重启失败告警 | **已修复**（DEV-009）：setup_system.sh 重启失败输出 [警告] |
| F-04/F-08 | **撤销说明（D-01 措辞修正，DEV-010）**：F-04 原归因错误（原表述"由 F-03 的 security_opt 覆盖"不成立——F-04 是 iptables 插入位置问题，与 security_opt 无关），其真实缺陷（iptables 插入位置偏移）已由 M4B-02 修复覆盖；F-08 原证据错误（check_env.sh 第 24 行已有 `tr -d '"'` 清理），撤销 |

## 2. 修复证据索引（dev-009；行号以提交 715033e 的当前代码为准，R-01 修订）

- M4B-01：`scripts/m4_dev009_verify.sh` 第 4 节 + 归档 `docs/verification/evidence/m4/m4_dev009_verify.log`（插入规则前缀、内核日志行、agent 解析 action=drop）
- M4B-02：`deploy/setup_firewall.sh` iptables 分支 `RULE_NO=$((ln - 1))`（第 130 行；-P 首行偏移 + 降序插入避免多规则位置漂移，R-04 修订）
- F-01：`deploy/setup_firewall.sh` nft 幂等检查（第 74-78 行）、iptables 幂等检查（第 119-123 行）+ 实测 1==1
- F-03：`deploy/docker-compose.yml` mem_limit/security_opt（第 37-39 行）+ compose config 退出码 0
- F-05/F-06/F-07：setup_firewall.sh 回滚 FAILED 计数（第 46-54 行）、移除 fw_log_handles.txt 文件操作、setup_system.sh journald 重启 if/else 告警（第 52-57 行）

## 3. 遗留与复验

- iptables-legacy 实机（M4B-02 的 LOG 位置）→ V-08
- journal 端到端（V1b-2）、fail2ban 真实库（bans 联调）、1C1G 资源（E-01~E-03）等 → VPS 复验清单（含新增 V-06/V-07/V-08 共 14 项）
- 多 LOG 重复计数语义：D-05 设计固有，部署手册 §8 已注明

## 4. 结论

M4 放行条件 7 项修复全部落地（附证据），审计通过放行；VPS 端按复验清单执行。
